# Backend environment example (do NOT commit real secrets)
# Priority order (application.yml):
# 1. SPRING_DATASOURCE_URL / USERNAME / PASSWORD
# 2. DB_URL / DB_USER / DB_PASSWORD (alias)
# 3. Local default jdbc:postgresql://localhost:5432/sameboat (postgres/postgres)

# Primary (recommended)
SPRING_DATASOURCE_URL=jdbc:postgresql://YOUR-NEON-HOST:5432/sameboat?sslmode=require
SPRING_DATASOURCE_USERNAME=sameboat_app
SPRING_DATASOURCE_PASSWORD=REPLACE_ME

# Optional alias (leave commented unless you prefer the shorter form)
# DB_URL=jdbc:postgresql://sameboat_app:REPLACE_ME@YOUR-NEON-HOST:5432/sameboat?sslmode=require
# DB_USER=sameboat_app
# DB_PASSWORD=REPLACE_ME

# Active Spring profile (uncomment if you want explicit)
# SPRING_PROFILES_ACTIVE=dev
