spring.application.name=sameboat-backend
# Datasource configuration moved to application.yml using env-driven defaults.
# Set environment variables DB_URL / DB_USER / DB_PASSWORD (or rely on defaults).
# (Removed spring.datasource.url/username/password to prevent blank password override.)
