# Simplified test overrides. Using H2 in-memory with create-drop managed via YAML.
# Keep only properties that must override defaults explicitly.

spring.jpa.hibernate.ddl-auto=create-drop
spring.flyway.enabled=false

# (Intentionally NOT setting spring.datasource.* here; application-test.yml provides it.)
